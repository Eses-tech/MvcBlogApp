@using MvcBlogApp.Models
@model List<BlogModel> 
@{
    ViewBag.Title = "Home Page";
}
<style>
    img{
        width:600px;
    }
</style> 
<h2>Blog Listesi</h2>


<div class="row">
    <div class="col-md-8">

        @foreach (var item in Model)
        {
            @*<div class="blogItem">
                <h3 class="page-header">@item.Header</h3>
                <p>
                    <span class="glyphicon glyphicon-time">
                        @item.UploadDate
                    </span>
                </p>
                <hr />
                <img class="img-responsive" src="https://via.placeholder.com/900x300" />
                <p>
                    @item.Explanation
                </p>
                <button class="btn btn-primary">Daha Fazla...</button>
            </div>*@
            //--------------------------------------------------------------------
        <div class="card" style="width: 18rem;">
            <h5 class="card-title">@item.Header</h5>
           
            <img class="card-img-top" src="https://via.placeholder.com/900x300" alt="Card image cap">
            <h6>&nbsp;</h6>
            <div class="card-body">

                <p class="card-text">@item.Explanation</p>

                <p>
                    <span class="glyphicon glyphicon-time">
                        @item.UploadDate
                    </span>
                </p>
                <button class="btn btn-primary">Daha Fazla...</button>
                <hr />
            </div>

        </div>
       
        }

    </div>

    <div class="col-md-2 col-md-offset-1">
        <div class="input-group">
            <div class="form-outline">
                <input type="search" id="form1" class="form-control" />
                <label class="form-label" for="form1">Search</label>
            </div>
            <button type="button" class="btn btn-primary">
                <i class="glyphicon glyphicon-search"></i>
            </button>
        </div>
    </div>

</div>

